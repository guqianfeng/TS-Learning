/**
 * tsc --init
 * tsc + 文件名 则 不走配置文件
 * 直接tsc即可
 * 
 * compilerOptions - removeComments 删除注释
 * includes（与compilerOptions同级）是个数组，编译数组指定的文件 和 files差不多应该
 * excludes （与compilerOptions同级）是个数组，编译数组以外的文件  
 */